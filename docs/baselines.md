# Baselines

`promptdrift baseline` produces a human-readable `promptdrift.baseline.json` with output hashes, assertion outcomes, and metrics. It never overwrites an existing baseline without `--force`. Raw outputs and secrets are not stored.
