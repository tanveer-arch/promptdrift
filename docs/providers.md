# Providers

OpenAI reads `OPENAI_API_KEY` by default and calls Chat Completions. Ollama defaults to `http://localhost:11434`. The `mock` provider is local and deterministic. Provider failures do not display request bodies or credentials.
